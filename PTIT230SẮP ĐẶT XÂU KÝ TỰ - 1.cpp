#include<iostream>
#include<string>
#include<algorithm>
using namespace std;
int main()
{
	int t;
	cin >> t;
	while (t--)
	{
		string a;
		cin >> a;
		int m = a.size();
		char* s = new char[m];
		sort(s, s + m);

	}
	return 0;
}